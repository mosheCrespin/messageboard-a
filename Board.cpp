#include "Board.hpp"
#include "Direction.hpp"
#include <string>
#include <iostream>
using ariel::Direction;
using namespace ariel;
using namespace std;
Board::Board(){}

    void Board::post(unsigned int row, unsigned int col, Direction direction, const string &str){}
    string Board::read(unsigned int rows, unsigned int col, Direction direction, unsigned int length){
       return "";
    }
    void Board::show(){}